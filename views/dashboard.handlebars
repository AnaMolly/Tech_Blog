<div >
    <h1 style="margin-bottom:30px"> Your Posts </h1>
    <div  class="postlist">
        {{#each posts as |post|}}
         <div class="card dashboard-post" style="width: 52rem; margin:40px 0px 40px 0px">
            <div class="card">
               <a href="/post/{{post.id}}"> <h5 class="card-header">{{post.post_title}}</h5></a> 
                <p class="card-text" style="padding: 12px 20px 12px 20px; margin:0px">{{post.post_content}}</p>
                <p class="card-text" style="padding: 12px 20px 12px 20px; margin:0px">Created on <strong>{{format_date post.createdAt}}</strong> 
            </div>
        </div>
    {{/each}}
    </div>
    
     <a href="/addpost" class="btn btn-primary"style="margin-top:20px">Add New Post</a>



</div>